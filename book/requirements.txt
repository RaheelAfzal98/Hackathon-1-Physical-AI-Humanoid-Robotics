# Requirements for Docusaurus documentation site (no Python dependencies needed for static site)
# This file exists in case you want to add Python tools for documentation generation
# For basic Docusaurus deployment, no Python dependencies are required
